<section class="featured" id="work">
  <div class="copy">
    <p class="eyebrow">Featured work</p>

    <h2>Craftsmanship you can see and feel.</h2>

    <p class="description">
      Premium engraved pens, metal business cards, glassware, leather goods,
      and custom pieces designed to make a lasting impression.
    </p>

    <a href="/portfolio" class="link">Explore the portfolio →</a>
  </div>

  <div class="showcase">
    <div class="showcase-card">
      <p>Featured product photography coming next.</p>
    </div>
  </div>
</section>

<style>
  .featured {
    display: grid;
    grid-template-columns: 0.85fr 1.15fr;
    align-items: center;
    gap: 5rem;
    max-width: 1400px;
    margin: 0 auto;
    padding: 8rem 5%;
    background: #111111;
    color: #ffffff;
  }

  .copy {
    max-width: 560px;
  }

  .eyebrow {
    margin: 0 0 1rem;
    color: #a0a0a0;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  h2 {
    margin: 0;
    font-size: clamp(2.8rem, 5vw, 5rem);
    line-height: 0.98;
    letter-spacing: -0.055em;
  }

  .description {
    margin: 1.75rem 0 0;
    color: #b8b8b8;
    font-size: 1.1rem;
    line-height: 1.7;
  }

  .link {
    display: inline-block;
    margin-top: 2rem;
    color: #ffffff;
    font-weight: 600;
    text-decoration: none;
  }

  .link:hover {
    color: #d7b46a;
  }

  .showcase-card {
    display: grid;
    place-items: center;
    min-height: 620px;
    border-radius: 32px;
    background:
      radial-gradient(circle at top right, #343434, #171717 60%);
    color: #8e8e8e;
  }

  @media (max-width: 900px) {
    .featured {
      grid-template-columns: 1fr;
      gap: 3rem;
      padding-top: 6rem;
      padding-bottom: 6rem;
    }

    .showcase-card {
      min-height: 420px;
    }
  }
</style>
