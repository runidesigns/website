<section class="contact-cta" id="contact">
  <div class="inner">
    <p class="eyebrow">Start your project</p>

    <h2>Have something in mind?</h2>

    <p class="description">
      Tell us what you would like engraved, the quantity, and your ideal
      timeline. We’ll help you turn the idea into something memorable.
    </p>

    <div class="actions">
      <a class="primary" href="mailto:brian@runidesigns.com">
        Email Runi Designs
      </a>

      <a class="secondary" href="/#quote">
        Request a Quote →
      </a>
    </div>
  </div>
</section>

<style>
  .contact-cta {
    padding: 9rem 5%;
    background: #f5f5f7;
  }

  .inner {
    max-width: 900px;
    margin: 0 auto;
    text-align: center;
  }

  .eyebrow {
    margin: 0 0 1rem;
    color: #777777;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  h2 {
    margin: 0;
    font-size: clamp(3rem, 7vw, 6rem);
    line-height: 0.98;
    letter-spacing: -0.055em;
  }

  .description {
    max-width: 680px;
    margin: 1.75rem auto 0;
    color: #666666;
    font-size: 1.15rem;
    line-height: 1.7;
  }

  .actions {
    display: flex;
    justify-content: center;
    gap: 1.25rem;
    margin-top: 2.5rem;
    flex-wrap: wrap;
  }

  .primary,
  .secondary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 52px;
    padding: 0 1.4rem;
    border-radius: 999px;
    font-weight: 600;
    text-decoration: none;
  }

  .primary {
    background: #111111;
    color: #ffffff;
  }

  .primary:hover {
    background: #2a2a2a;
  }

  .secondary {
    color: #111111;
  }

  .secondary:hover {
    color: #8a6a2f;
  }

  @media (max-width: 700px) {
    .contact-cta {
      padding-top: 6rem;
      padding-bottom: 6rem;
    }

    .actions {
      flex-direction: column;
      align-items: stretch;
    }
  }
</style>